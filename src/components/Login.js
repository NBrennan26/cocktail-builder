import React from "react";

function Login() {
  return (
    <div id="login">
      <button id="login-btn" type="button" onClick={() => console.log("Click")}>
        Log in
      </button>
    </div>
  );
}

export default Login;
